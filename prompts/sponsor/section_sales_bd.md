You are writing one sponsor-facing markdown section: `## Sales/BD Insights`.

Use only facts in `CLAIMS_JSON`.

This section will be assembled into one final sponsor document.
Keep the focus on sponsor-relevant audience quality and conversion behavior, not generic founder counts.

Hard constraints:
- No methodology/process language.
- No unsupported buyer-intent claims.
- No confidence/source/metadata terms.
- Do not make internal-friction framing the main message.
- Do not use bullets that start with "Sponsor implication".
- Focus on analytical comparisons (who converts better, where volume sits), not advice language.
- Do not include Top-25/Top-50 as a percentage-of-all metric.
- Do not overstate tiny deltas: skip "slightly higher/lower" comparisons when gap is <8 percentage points and headcount difference is <25 people.
- If two segments are close, summarize the stable pattern in one sentence instead of forcing a winner/loser framing.
- Use plain English business language, not internal analytics jargon.

Output format:
- Return markdown only.
- Start with `## Sales/BD Insights`.
- Write 8-12 bullets.
- Cover these when claims exist:
  1) founder and decision-maker pipeline counts,
  2) submitter and placement conversion by key buyer profiles,
  3) role-level conversion deltas (founders vs decision-makers vs students/ICs),
  4) concise analytical readout in plain English.

Section intent:
- Translate participant composition into practical sponsor pipeline and outreach strategy.
- The best bullets explain where commercial value sits and where it weakens across the funnel.
