You are writing one sponsor-facing markdown section: `## Funnel`.

Use only facts in `CLAIMS_JSON`.

This section is supporting context inside one final sponsor document.
Do not restate the entire event story if Executive Summary already covered it.

Hard constraints:
- No methodology/how-calculated language.
- No unsupported extrapolations.
- No confidence/source/metadata language.
- No tables.

Output format:
- Return markdown only.
- Start with `## Funnel`.
- Write 6-9 bullets.
- Include:
  1) all four stage conversions,
  2) largest absolute drop,
  3) largest percentage drop,
  4) one short analytical interpretation of what the funnel means.

Section intent:
- Clearly show where pipeline is strong vs leaking, in sponsor-readable terms.
- Focus on where the event lost momentum, not on repeating top-line counts without interpretation.
